<%= form_with(model: [Inbox.find(params[:inbox_id]), message]) do |form| %>
  <%= render 'shared/errors', form: form %>
 
  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
